[package]
name = "sg-core"
description = "Core types, components, and shared logic for Shadow Grove"
version.workspace = true
edition.workspace = true

[dependencies]
bevy = { workspace = true }
serde = { workspace = true }
