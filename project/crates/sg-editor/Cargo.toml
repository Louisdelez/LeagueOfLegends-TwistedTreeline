[package]
name = "sg-editor"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "sg-editor"
path = "src/main.rs"

[dependencies]
bevy = "0.18"
sg-core = { path = "../sg-core" }
sg-map = { path = "../sg-map" }
serde_json = "1"
serde = { version = "1", features = ["derive"] }
